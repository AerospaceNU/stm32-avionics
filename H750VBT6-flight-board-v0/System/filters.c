/*
 * filters.c
 */
#include "filters.h"

static FilterData_t filterData;

void applyFilterData(SensorData_t* curSensorVals) {
	// TODO: Fill this in. Will likely combine many internal functions into one
}

FilterData_t* getFilteredData() {
	return &filterData;
}

